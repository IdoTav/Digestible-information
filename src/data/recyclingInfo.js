import recyclingBinIcon from '../assets/icons/recycling-bin.png'

// Full-color illustration (with its own baked-in black icon/white text, not
// black line-art on transparent like storage/manufacturer's icons), so it
// isn't inverted in high-contrast mode.
export const recyclingBodyIcon = {
  icon: recyclingBinIcon,
  iconWidth: 122,
  iconHeight: 166,
  invertOnHighContrast: false,
}
